import { style } from '@vanilla-extract/css';

export const droppableContainer = style({
  minHeight: '20rem',
  height: '100%',
  display: 'flex',
  flexDirection: 'column',
  position: 'relative',
});
